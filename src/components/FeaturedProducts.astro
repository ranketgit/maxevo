---
// src/components/FeaturedProducts.astro

const products = [
    { 
        name: "Cures-dents individuels", 
        category: "Hygiène", 
        desc: "Palillos ronds à double pointe, emballés individuellement.", 
        image: "/products/1.jpg" 
    },
    { 
        name: "Bol en kraft naturel", 
        category: "Contenants", 
        desc: "Format moyen pour salades ou accompagnements.", 
        image: "/products/2.jpg" 
    },
    { 
        name: "Pot à soupe avec couvercle", 
        category: "Plats chauds", 
        desc: "Design hermétique avec trous d'aération pour la vapeur.", 
        image: "/products/3.jpg" 
    },
    { 
        name: "Étui sandwich triangulaire", 
        category: "Snacking", 
        desc: "Fenêtre transparente pour une visibilité optimale.", 
        image: "/products/4.jpg" 
    },
    { 
        name: "Boîte Click-Lock Kraft", 
        category: "Take-away", 
        desc: "Fermeture sécurisée pour plats cuisinés.", 
        image: "/products/5.jpg" 
    },
    { 
        name: "Porte-gobelets 4 cavités", 
        category: "Accessoires", 
        desc: "Fibre moulée robuste pour un transport stable.", 
        image: "/products/6.jpg" 
    },
    { 
        name: "Sachet kraft plat", 
        category: "Boulangerie", 
        desc: "Idéal pour viennoiseries et produits secs.", 
        image: "/products/7.jpg" 
    },
    { 
        name: "Gobelet avec bague isolante", 
        category: "Boissons", 
        desc: "Protection thermique pour boissons très chaudes.", 
        image: "/products/8.jpg" 
    },
    { 
        name: "Couvercle biodégradable", 
        category: "Accessoires", 
        desc: "Fermeture écologique pour bols et contenants ronds.", 
        image: "/products/9.jpg" 
    },
    { 
        name: "Barquette ouverte en carton", 
        category: "Snacking", 
        desc: "Parfaite pour frites, nuggets ou street food.", 
        image: "/products/10.jpg" 
    }
];
---

<section class="py-16 md:py-24 bg-white w-full overflow-hidden" id="produits">
    <div class="container mx-auto px-4 md:px-8 max-w-7xl">
        
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-12 gap-6">
            <div>
                <h2 class="text-3xl md:text-4xl font-normal text-[#333333] mb-2 tracking-wide">
                    Nos produits populaires 
                </h2>
                <p class="text-[#333333] opacity-60 font-light text-lg">
                    Glissez pour découvrir nos solutions.
                </p>
            </div>
            
            <a href="/#products" class="bg-[#E86B21] text-white px-8 py-4 rounded-xl font-medium hover:bg-[#d15f1c] transition-colors flex items-center gap-3 group shadow-lg shrink-0">
                Découvrir tout le catalogue
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 group-hover:translate-x-1 transition-transform" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                </svg>
            </a>
        </div>

        <div class="relative w-full">
            <div 
                id="product-slider" 
                class="flex gap-6 overflow-x-auto hide-scrollbar snap-x snap-mandatory cursor-grab active:cursor-grabbing pb-8"
            >
                {products.map((product) => (
                    <div class="product-card shrink-0 w-[280px] sm:w-[320px] snap-start flex flex-col select-none group">
                        <div class="w-full aspect-[4/5] bg-[#F9F9F9] rounded-xl mb-4 overflow-hidden border border-gray-100 transition-colors pointer-events-none">
                            <img 
                                src={product.image} 
                                alt={product.name} 
                                class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                                loading="lazy"
                            />
                        </div>
                        <span class="text-xs text-[#333333] opacity-50 uppercase tracking-widest mb-1">{product.category}</span>
                        <h3 class="text-xl font-normal text-[#333333] mb-1">{product.name}</h3>
                        <p class="text-sm font-light text-[#333333] opacity-60">{product.desc}</p>
                    </div>
                ))}
            </div>
        </div>

    </div>
</section>

<style>
    .hide-scrollbar::-webkit-scrollbar {
        display: none;
    }
    .hide-scrollbar {
        -ms-overflow-style: none;
        scrollbar-width: none;
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const slider = document.getElementById('product-slider');
        if (!slider) return;

        let isDown = false;
        let startX: number;
        let scrollLeft: number;
        let autoPlayInterval: number;

        const stopAutoPlay = () => {
            if (autoPlayInterval) {
                clearInterval(autoPlayInterval);
            }
        };

        // Fixed the interval stacking bug by clearing it first
        const startAutoPlay = () => {
            stopAutoPlay(); 
            autoPlayInterval = window.setInterval(() => {
                const card = slider.querySelector('.product-card') as HTMLElement;
                const cardWidth = card ? card.offsetWidth + 24 : 300; 
                
                if (slider.scrollLeft >= (slider.scrollWidth - slider.clientWidth - 10)) {
                    slider.scrollTo({ left: 0, behavior: 'smooth' });
                } else {
                    slider.scrollBy({ left: cardWidth, behavior: 'smooth' });
                }
            }, 3000);
        };

        slider.addEventListener('mousedown', (e) => {
            isDown = true;
            startX = e.pageX - slider.offsetLeft;
            scrollLeft = slider.scrollLeft;
            stopAutoPlay();
        });

        slider.addEventListener('mouseleave', () => {
            isDown = false;
            startAutoPlay();
        });

        slider.addEventListener('mouseup', () => {
            isDown = false;
            startAutoPlay();
        });

        slider.addEventListener('mousemove', (e) => {
            if (!isDown) return;
            e.preventDefault();
            const x = e.pageX - slider.offsetLeft;
            const walk = (x - startX) * 2; 
            slider.scrollLeft = scrollLeft - walk;
        });

        slider.addEventListener('touchstart', stopAutoPlay, { passive: true });
        slider.addEventListener('touchend', startAutoPlay);

        startAutoPlay();
    });
</script>